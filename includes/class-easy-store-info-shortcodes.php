<?php
/**
 * Shortcodes shim — placeholder for future split of shortcodes out of main class.
 * Currently the main class (`class-easy-store-info.php`) registers the shortcodes.
 */
if ( ! defined( 'ABSPATH' ) ) {
    exit;
}
